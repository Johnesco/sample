# Golden seeds for deterministic walkthrough testing
# Format: engine:seed:binary_hash:date
# binary_hash = first 8 chars of sha256 of compiled game binary
# Re-discover with: python tools/testing/find_seeds.py --config tests/project.conf
#
# Inform 7 (.ulx via glulxe):
# Game is deterministic (no RNG-dependent puzzles), seed 1 works trivially
glulxe:1:ef377694:2026-03-05
